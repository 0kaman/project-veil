# Veil — Decisions log

> Append-only, dated. Each row: the decision, the why, and where it lives. Newest
> first. This is the project's memory — read it before re-litigating a choice.
>
> **This log restarts at the reboot (2026-07-15).** v1's log — 59 rows, back to
> the raw-CDP choice — is preserved in git at `9e9f3e0` and is still worth
> reading; the rows below supersede it where they conflict. The evidence that
> forced the reboot is recorded here, because the code that produced it is gone.

## The reboot

| date | decision | why | where |
|---|---|---|---|
| 2026-07-26 | **An open dialog is reported — in the lean view AND in the act diff — when it is actually holding the page.** Two signals, because either alone misses a real case: nothing interactive outside the dialog, OR ≥50% of the AX tree hidden by `ariaHidden*`/`inert*`. | **Found by asking why 5 of 6 fare runs diverged: one failure was in ALL SIX, including the run that succeeded.** `veil_do combobox-where-from` → ok, then `combobox-where-to` → NOT-FOUND, costing 1–2 steps every run when two of them died at the step cap. **My first hypothesis was wrong and the probe caught it**: I assumed the field was renamed and was about to anchor ids to `backendDOMNodeId` — but the ids differ (906 → 864), so it is a different DOM node, and that fix would have done nothing. What actually happens: typing opens `dialog "Enter your origin"` and the destination field goes `present=true ariaHidden=true visible=true`. **Perception was correct all along** — refusing to act on an element behind a modal is right — we simply never said a modal was open, so the agent read its own dialog as a broken page. **Fourth time this session the evidence was ours and the interpretation was withheld** (unnamed doers, the submit verb, the backdrop, now modal state). The detection needed two signals because a naive "any dialog node" fired on a `role="dialog"` promo that blocks nothing (caught by my own fixture), while strict containment missed Google, whose autocomplete listbox is portalled to `<body>` OUTSIDE the dialog. Separation is measured, not tuned: Google with the dialog open hides **508 of 561** nodes; a non-blocking promo hides **0**. Fixture note: `aria-hidden` does NOT remove focusable descendants from the AX tree (Chrome treats it as invalid), so the fixture had to use `inert` — which reports as `inertElement`. | `@veil/core` `graph/model.ts` (`MODAL_ROLES`, `meta.dialog`), `pipeline/stage-1-axtree.ts` (`findBlockingDialog`), `graph/project.ts`, `graph/diff.ts`; `@veil/mcp` `format.ts`; `__tests__/graph.test.ts`, `integration/act.integration.test.ts` |
| 2026-07-26 | **The playground collapses old tool bodies to their receipts before sending, and the saving is measured by REPLAY, not by comparing two live runs.** The receipt always survives; a tool message is rewritten, never removed. | **93% of a run's prompt spend was re-sent history.** The run that answered the fare task billed **1,561,545** prompt tokens against a final context of 41,361 — held flat it would have been ~116,000. `veil_read` bodies were 70% of the tool output feeding that (90,377 chars vs `veil_query` 13,565 and `veil_open` 3,846), which also says plainly that **the perception design is not the cost**: open+query together are ~4,300 tokens across a whole run, so v1's 18k-per-dump is genuinely solved. The recovery was already in the architecture — a truncated read returns a HANDLE precisely so its text can be re-pulled, making a body in turn 7 redundant by turn 40. **Measured deterministically over 14 recorded conversations with the pruner as the only variable: 8,730,961 → 2,602,045 tokens, 70.2%**, peak context 91,033 → 53,852 on the worst case. A live A/B was deliberately NOT used as the evidence: the agent is non-deterministic, so the two arms took different paths (28 `veil_query` vs 14, 5 `veil_read` vs 16) and the totals would mostly measure that. The live arm is a mechanical check only — 56 prune events, no malformed requests, no re-pull confusion — and its fair statistic is per-call context, **30,313 → 7,196**. The estimator is char/4 and runs 18% under what the API actually billed; it is applied identically to both arms so the ratio holds, and the benchmark prints the discrepancy rather than hiding it. | `@veil/playground` `prune.ts`, `agent.ts`, `trace.ts` (`prune` event); `bench/prune-benchmark.ts`, `__tests__/prune.test.ts` |
| 2026-07-26 | **A session read uses `denseExtract`: keep every candidate container that clears the bar, drop any that contains another, join in document order — and flatten with separators at block boundaries.** Scoped to `via: session` only; the fetch path is untouched. | **A driven page is usually a LIST OF RECORDS, and both existing extractors pick ONE best container and flatten it.** Measured on a live Cleartrip results page: the container holding the flight rows scored **3,519 and lost to a 4,070 promo block**, so the read returned `Airline / Departure / Duration / Arrival / Price` followed by bare `₹8,771` three times — the rows sat in the DOM fully formed (`IndiGo 6E-312 09:15 4h 40m 1 stop 13:55 ₹8,771`) and neither surface could see them. Separators matter as much as selection: `textContent` concatenates siblings with nothing between them (`"Trip typeDeparture"`), which is not two words to any counter and is gibberish to a model — 4,070 characters were counting as 93 words. Result: **93 → 659 words, with complete ordered rows.** Two things I tried first and discarded on evidence: raising the `fallbackRaw` gate (the fallback returns 584 words of *promos*, so it would not have helped), and enriching node names with row text (it is a record, not a name — it would churn ids on every price change and inflate the lean view). Session-scoped deliberately: on a fetched article this rule drags in the comment section, and the fetch extractors are tuned against a 60-page corpus with a geeksforgeeks regression that must not move. | `@veil/read` `extract.ts` (`denseExtract`, `blockish`), `read.ts`; `__tests__/read.test.ts` |
| 2026-07-26 | **`type` REPLACES a field's contents instead of appending, and every value-bearing action reads back what the field now holds.** `clear` remains for an explicit empty. **WITHDRAWN with this row: my claim that the AX tree cannot perceive type-ahead.** | **One input bug, misdiagnosed twice as a perception bug.** Google Flights pre-fills the origin by geolocation; typing `BLR` produced **`BLRBengaluru`**, which matches no airport. The live AX tree then read `StaticText "No matching locations found"`, the graph collapsed 71 nodes → 2, and I concluded the AX tree "genuinely does not contain the options" and that this "lands directly on the founding choice — breaks on type-ahead". **Both wrong.** The collapse was CORRECT perception: 494 of 517 ignored nodes were `ariaHiddenSubtree`, because the interaction opened a modal and Google aria-hides the rest of the page. And once the search actually matched, the tree exposed exactly what was needed: `listbox` + `option "Kempegowda International Airport Bengaluru (BLR)"`. The AX tree was never at fault. What was: an agent reads `type` as fill (Playwright splits `fill()` from `type()`; we have one verb, used as fill), and the receipt said `ok` while the field held garbage — so the damage surfaced far downstream wearing someone else's face. Hence the second half: the receipt now echoes the value read back from the page, because a field can reject or reformat what it was sent. Every existing test typed into an EMPTY field, which is why this survived. | `@veil/core` `browser/interact.ts` (`CLEAR_FN`, `READ_VALUE_FN`), `session.ts` (`ActResult.value`), `@veil/mcp` `format.ts`; `integration/act.integration.test.ts` |
| 2026-07-26 | **Every session enables `Emulation.setFocusEmulationEnabled`. Two launcher flags I reached for first were REMOVED because measurement said they did nothing.** The actionability check's rAF wait also gained a `setTimeout` fallback, scoped to guard the branch where focus emulation throws. | **A live run lost 60s of 364s to two 30-second hangs.** Reproduced deterministically: with four sessions open, a click on a non-foreground tab took **30,003ms** and failed `dispatch-failed — CDP timeout: Runtime.callFunctionOn`, against **230ms** on the sole open tab. Cause: the check awaits two `requestAnimationFrame`s and an occluded renderer never fires them. Fixing that left a flat **5,467ms per mouse event**, and isolating by action kind found why — `focus`/`clear`/`type` (no Input domain) ran ~470ms while `hover` and `click` both cost 5,467ms, `hover` being a SINGLE `mouseMoved`: `Input.dispatchMouseEvent` waits on a compositor ack that an occluded renderer never sends. Focus emulation makes the page believe it is frontmost **without switching tabs**, which would serialise the concurrency the pool exists to provide. **The flags were then tested rather than assumed** — focus-emulation-without-flags: 230ms across every action, identical to with them; rAF-fallback-alone: 812ms focus / 6,009ms click. They earned nothing and were deleted. Every session is "the front tab" as far as its agent is concerned, but only one really is — this is the general form of that, not a fix for one site. | `@veil/core` `session.ts`, `browser/interact.ts`; `integration/session.integration.test.ts` |
| 2026-07-26 | **`OBSCURED` names a RECOVERY, not just the blocker.** Two cases, distinguished by evidence: an overlay containing a dismiss-looking control now reports the **node id** to click first (`dismiss it first: veil_do button-no-thanks`), matched against the graph so it is always addressable; and a **backdrop** — class matching `backdrop\|scrim\|mask\|veil`, or a blocker covering ≥90% of the viewport — is named as such, with an explicit instruction NOT to hunt for a close control. | **"covered by `<div class=hsBackDrop>`" is true and useless: an agent cannot address a raw div.** Measured — given exactly that, a live agent ran four `veil_query` calls guessing at `"close"`, `"Close"` and `"hsBackDrop"`, matched nothing, and abandoned MakeMyTrip entirely. A blocker it cannot act on is a dead end; a node id is a next move. The dismiss list is filtered to genuine exits (`close/dismiss/no thanks/not now/skip/got it/×`), so a promo's "Subscribe" is never offered as a way out. **The backdrop half exists because the fix alone did not cover the observed cases:** probed live afterwards, both `hsBackDrop` (MakeMyTrip) and `calendar--backdrop` (Cleartrip) contain no controls at all — they are the dimming layer behind an open widget, so the honest advice is "finish or cancel what is open; the control you want is inside it, not under it". Fixture note, twice over: the promo first landed on the JS search box and broke the submit test, and `#cover` is correctly NOT a backdrop — small and unclassed — so the backdrop case needed its own element carrying Cleartrip's real class name. | `@veil/core` `browser/interact.ts` (`ACTIONABLE_FN`, `DispatchResult.dismiss/backdrop`), `session.ts`; `integration/act.integration.test.ts` |
| 2026-07-26 | **`veil_read` accepts an open SESSION id, and the session tier is classified on its own terms.** `SessionPool.html()` returns the live document; `Reader.readHtml()` extracts it through the same pipeline, budget and handle as a fetched page, reporting `via: session`. Because a driven page has **no rung above it**, that tier never returns `js-shell` (the JS has already run — escalating would send the agent somewhere it has already been), and a thin page keeps its text with a note instead of being emptied. A bot challenge is still `doorman`, because that one IS actionable: pick another source. | **The real-task run failed on exactly this.** An agent drove Cleartrip's booking form end to end — date, non-stop, search, 2,417ms settle — reached a live results page with 9 IndiGo matches, then called `veil_read {url: "s2", query: "flight details near button-book"}` and got **FETCH-FAILED**. It had the answer on screen and no way to read it. Re-fetching the URL is not a substitute: those results exist only because of the form state in that tab. The principle "the graph stays pure behaviour, prose comes only from veil_read" is right, but it left the ladder one rung short — **after you act, the answer IS prose, and it lives nowhere but the session.** Note the agent invented the correct call shape unprompted, passing the session id where a URL goes, which is what settled the API. Verified on the page that failed: drive the form, then `via: session · ok · 2,703 words` returning real fares (`cheapest Flight 10 Aug 08:00-11:50 from ₹8,874`). | `@veil/core` `session.ts` (`html()`), `@veil/read` `read.ts` (`readHtml`, session-tier classification) + `receipt.ts` (`Via`), `@veil/mcp` `tools.ts`; `integration/session.integration.test.ts`, `read/__tests__/read.test.ts` |
| 2026-07-26 | **An affordance belongs on the NODE, not in the tool description.** A node that submits a form which no clickable node reaches is marked `submitOnly`, and both renderings — the lean view and `veil_query` — print `(action:"submit")` after its effect. | **A live model was handed Hacker News' search box and reported: *"the missing capability is a submit action on the textbox"* — while `action:"submit"` was in the `veil_do` enum it had been given, and the tool description said, in those words, "use action='submit' to send a search or form that has NO submit button". Checked before concluding: the built server was three minutes newer than the trace and contained both.** So this is not a stale build and not a dim model — **an agent decides WHAT TO DO from the graph, and only then reaches for a tool**. An affordance that lives solely in the tool schema is invisible at the moment of the decision. That is a direct qualification of "the surface IS the router": the router has to be the artifact the agent is reading. The evidence was already ours — HN's textbox carries `{type:"submit", category:"form_submit"}`, which is where its `→ GET //hn.algolia.com/` came from; we held the fact and withheld the verb. Scoped by evidence so it cannot become noise: the hint appears only when NO clickable node reaches the same target. Verified live — HN shows it, github/login's six fields do not, because `button-sign-in` already covers `POST /session`. | `@veil/core` `graph/model.ts` (`submitOnly`, `CLICKABLE`), `graph/build.ts`, `graph/project.ts`; `@veil/mcp` `format.ts`; `__tests__/graph.test.ts`, `integration/graph.integration.test.ts` |
| 2026-07-25 | **Two fixes a clean playground run earned: (1) a `submit` action — a REAL Enter keypress, not `form.requestSubmit()`; (2) refusals stop naming a node that navigation has removed (`nodeGone`).** `submit` focuses the node, optionally types a value, then presses Enter with full key identity. The reachability check lives in `session.replay`, where the graph and config are both visible, and covers all three sites that said "use veil_do": the gate, `stale-token`, and the 403 note. | **Both came from the first uncorrupted run of the smoke prompt, and the model diagnosed one of them.** (1) It typed into Hacker News' search box, tried to press Enter by typing `"\n"`, got "nothing observably changed", and reported the gap in its own words — *"the missing capability is a way to trigger the search"*. It also asked us to "expose a search trigger button", which is the wrong fix: HN has no button, only implicit submission. A real keypress covers BOTH shapes; `requestSubmit()` covers only the form one. **Key identity is essential and easy to miss** — `text` alone inserts printable characters but does not trigger implicit submission, so an Enter without `key`/`code`/`windowsVirtualKeyCode` dispatches cleanly and does nothing. Verified on what the SERVER received, not on `ok:true`: button-less form → `/find?q=chrome+devtools`; no form at all → `/find?via=keydown&q=hello`; and live on HN → navigates to `/?q=chrome+devtools+protocol` and fires the Algolia query. (2) The model replayed a POST, was refused with "Use veil_do to perform it for real", **did exactly that, and got NOT-FOUND** — the submit had navigated to the response page and the button was gone. The replay cache is keyed by node id and outlives the node, so the refusal is reached normally and the advice reads confident while being impossible to follow. Advice that cannot be followed is worse than none. | `@veil/core` `browser/interact.ts` (`pressEnter`, `submit`), `session.ts` (`nodeGone`), `@veil/mcp` `tools.ts` + `format.ts`; `integration/act.integration.test.ts` (both submit shapes), `integration/replay.integration.test.ts` (the removed node) |
| 2026-07-25 | **CORRECTION — there IS an input cap, and my previous row was wrong. `ink-text-input` is replaced by `PromptInput`, plus `--prompt-file` for long prompts.** The earlier row concluded "no upper cap; the prompt arrives intact, and the skipped steps are the model's doing". That conclusion was not supported by its own test. | **The test I ran only asked "does a newline-bearing chunk register as Return?" — it does not — and I generalised that into "the paste arrives intact".** The real defect is elsewhere: `ink-text-input` derives the next value from its `value` **PROP** and passes a finished string to `onChange`. A large paste reaches the TTY as SEVERAL data events; when two land before React re-renders, the second rebuilds from the pre-paste prop and **the first chunk is silently lost** — in contiguous blocks, exactly matching a 16-step script that ran 4-7 and 13-16 while dropping 1-3 and 8-12. The visible symptom is a screen of overwritten text: a multi-line string in a one-line box breaks Ink's frame-height maths so stale frames are never cleared. Fix: the value lives in a **ref**, updated synchronously, so concurrent chunks compose; only a BARE Return submits; newlines are kept in the value and flattened for display. Tested through a pure reducer, because the race is unreachable from any test that drives a real terminal — including "two chunks in one tick compose" and a 12KB paste. **So the previous run's skipped steps were the harness, not the model** — and the earlier verdict on model compliance is withdrawn with them. | `@veil/playground` `ui/PromptInput.tsx` (new), `ui/App.tsx`, `config.ts` (`--prompt-file`), `__tests__/prompt-input.test.ts` |
| 2026-07-25 | **The playground traces the user's turn VERBATIM.** `llm.request` recorded only `messages.length`, and `run.start.goal` reads `"(interactive)"` for a typed turn — so the prompt was nowhere in the trace. Now a `user` event carries the full text plus `chars`. ~~Also checked and RULED OUT: there is no upper cap on the input prompt.~~ **WRONG — see the correction above; there is one, and it was shredding pasted prompts.** | A run executed steps 4-7 and 13-16 of a 16-step script and silently dropped 1-3 and 8-12, and the trace could not distinguish *"the model ignored the instructions"* from *"the terminal mangled the paste"* — which is the whole point of having a trace. I suspected the input path (`ink-text-input` is single-line, and its `key.return` branch calls `onSubmit` and discards the rest of the chunk) and **tested it instead of assuming: a pasted chunk containing newlines parses with `key.return=false`** (`hello\nworld` → `name=''`), so Ink appends it whole and never submits early. No `slice` on the send path either. The paste arrives intact, so the skipping is the model's, not the harness's. The test that matters is the JSONL round-trip: a multi-line, quote- and backslash-bearing prompt must come back byte-identical and must not split into several records. | `@veil/playground` `trace.ts` (`user` event), `agent.ts` (`send` emits before anything can transform it), `__tests__/trace.test.ts` |
| 2026-07-25 | **Three fixes the live playground found, all one shape: the graph asserting something the data contradicts.** (1) **A doer is defined by its ROLE, not by having a name** — the `if (n.name)` filter in `build.ts` is withdrawn. (2) **`pickPrimary` prefers the request carrying the typed VALUE**, falling back to mutations-then-arrival when the action had no value. (3) **`applyEdits` reports `unknownEdits`** — edits naming a field the captured request never had. | **Found by an LLM driving the real MCP server, not by a test.** (1) HN's site search is an unlabelled `<input>`, so the front page rendered `ACTIONS (0) — "nothing on this page is actionable"` while holding a node with `fires: GET //hn.algolia.com/`. Mistral read that and reported, correctly and uselessly, *"I am stuck because there is no actionable search control to type into."* The original rationale ("unnamed doers are unaddressable") is false: ids are stable and deduped (`textbox`, `textbox-2`) and `fires` describes the node. Cost measured before shipping: **0 unnamed doers on httpbin's 16-control form, on github/login, and on a wikipedia article; 1 on HN** — no token risk. (2) Typing into Wikipedia's search fires `api.php?action=cirrus-config-dump` (telemetry) **before** `rest.php/v1/search/title?q=behaviour+graph`; first-wins learned the telemetry call, and the later replay edited `search=` onto an endpoint with no such parameter. Match is on the DECODED url/body (`behaviour+graph`), or it silently never fires. Clicks carry no value and are untouched — the path verified on real sites (`POST /post`, `GET /reply`). (3) That same replay answered **`200` with `{"warnings":{"main":"Unrecognized parameter: search."}}`** and our receipt reported clean success. Verified after: HN shows the search box; Wikipedia learns the search API; replay returns real results; a bogus edit reports `unknownEdits: ["query:search"]`. | `@veil/core` `graph/build.ts`, `browser/capture.ts`, `browser/replay.ts`, `session.ts`; `@veil/mcp` `format.ts`; `integration/graph.integration.test.ts` (unnamed control), `__tests__/replay.test.ts` |
| 2026-07-25 | **A rejection only counts as token evidence when the replay carried NO caller edits** — third narrowing of the same rule, same day. Plus a TESTING RULE this slice earned: **any state that accumulates across calls needs a LOOP test and a test per SCHEME the state models.** A fixture that implements one scheme, exercised one iteration, cannot fail. | **Three defects in one slice, all the same mistake: treating a signal that isn't about the token as evidence about the token.** (1) success ⇒ spent — broke reusable tokens; (2) any 401/403/419/422 ⇒ spent — a Rails-style **422 "quantity must be positive" on an EDITED payload marked a valid session token spent, and the next clean replay, which returns 200, was refused without firing**. The guard is structural (did *we* change the payload?), not a phrase match, so it holds in any language. All three were invisible to a suite where every test did one act + one replay against a fresh session and one CSRF scheme. Final measured behaviour — reusable: 3/3 fire, all 200; validation-422: `#2` rejected, `#3` clean fires 200; single-use unedited: `#1 200` (no claim), `#2 403 DESYNC confirmed`, `#3-5 refused without firing`. RESIDUAL, stated: an unedited replay rejected for a non-token reason (stock gone, rate limit) still reads as a spent token — a false refusal, which is the cheap direction, and "re-perceive or veil_do" is the right move anyway when the server starts rejecting a request that used to work. | `@veil/core` `browser/replay.ts`, `session.ts`; `integration/replay.integration.test.ts` (`/reusable` + the 422 route); benchmark now checked in at `packages/core/bench/replay-benchmark.mjs` (`pnpm --filter @veil/core bench:replay`) so the ARCHITECTURE figure is reproducible |
| 2026-07-25 | **AMENDMENT, same day — the spent-token rule shipped in `a34d3e7` was WRONG and is replaced. A token is spent only on evidence the SERVER supplied.** The first cut marked a token spent because a replay *succeeded*, and inferred `desynced` from "the page still holds the value we sent". Both are now driven by rejection instead: a value moves to `spent` only when the server rejects it (401/403/419/422) **after it had previously worked**; `desynced` is claimed only on that same rejection, with the page still holding the value. The pre-fire refusal fires only against that confirmed-spent set. | **The inference was invalid, and `replay.ts`'s own probe table said so three lines above the code — session-scoped and rotating tokens are accepted repeatedly (`stale 200 · refreshed 200`), so on those the page holds the same token because it is STILL VALID, not because we burned it.** Django and Rails are that case: the majority. Measured against a reusable-token server, the shipped cut gave `replay #1 200 but desynced=true` (a **false statement in the receipt**) then `#2, #3 → refusal=stale-token, never fired` — each of which returns 200. It disabled repeat replay, the entire point of a ~55× path, on two schemes out of three. **The fixture only implemented single-use, so no test could catch it.** After the fix — reusable: 3/3 fire, all 200, `desynced=false`; single-use: `#1 200` (no claim), `#2 403 DESYNC confirmed`, `#3-5 refused without firing`. Cost is one wasted 403 per single-use node instead of zero: honest, and far cheaper than breaking the common case. | `@veil/core` `browser/replay.ts` (`TokenLedger`, `TOKEN_REJECTED`), `session.ts` (`tokens.worked` / `tokens.spent`), `@veil/mcp` `format.ts`; regression test `integration/replay.integration.test.ts` (the `/reusable` route) |
| 2026-07-25 | **REPLAY DESYNCHRONIZES A SINGLE-USE-TOKEN PAGE — handled by measurement, not by guessing at response shapes.** Two additions: (a) after a successful token-bearing replay, re-read the page; if it still holds the token we just spent, report `desynced` in the receipt; (b) before firing, refuse (`stale-token`) when every token we would send is one an earlier replay already consumed. **SUPERSEDED the same day by the amendment above — see it for why this was wrong.** REJECTED: parsing an app-specific `nextToken` out of the response and writing it back into the DOM — that is inference about a shape we have seen once, the exact move this project has repeatedly withdrawn. | **Found by a server-instrumented loop, and it is NOT the residual already documented.** 5 × (click, replay) against a local single-use-CSRF server, logging what the server actually saw: iter 1 click `200/ok`, replay `200/ok`; **iters 2-5 the CLICK itself got `403/spent`**, not just the replay. A real click completes the app's handshake (response → the page's own `.then` installs `nextToken`); replay fires through raw `fetch`, so that handler never runs — the token is consumed server-side and its successor dropped. The page is left holding a spent token and cannot recover on its own, because a 403 carries no new token. Distinct from the first residual ("the page never re-rendered, so refresh degenerates to stale"): `refreshed` was **non-empty** on every 403 row — the page did hand over something newer than the template; we had spent it. First measured after the mitigation: replay 1 `DESYNC`, replays 2-5 refused **without firing** (`0-1ms`, nothing left the browser). **My 7 green integration tests all missed this** — each did exactly one act and one replay against a fresh session; only a loop exposes it. | `@veil/core` `browser/replay.ts` (`tokenValues`, the pre-fire refusal, the post-fire re-read), `session.ts` (`spentTokens`), `@veil/mcp` `format.ts` |
| 2026-07-25 | **`veil_replay` ships, gated, with refresh-at-fire-time.** Config `replay: off \| safe \| all`, **default `safe`** (idempotent methods only); optional host allowlist. The gate is checked at tool REGISTRATION (mode `off` → the model never sees the tool) **and again at fire time** — defence in depth, since startup config is not the same thing as config at the moment a request leaves. | The gate is a **security** boundary, not a feature flag: Veil reads untrusted text from the open web and hands it to an LLM holding action capability and live cookies — a textbook confused-deputy chain, and replay is its sharpest edge (page text saying "replay the transfer with amount=10000" would execute authenticated, in milliseconds, with no DOM interaction a human would notice). The ~55× speedup is also a ~55× blast-radius multiplier, so the defaults are asymmetric: replaying a GET wastes a request, replaying a POST can charge a card. **Speed, measured on a LOCALHOST fixture, one act+replay per fresh session, over two runs: median click 235-330ms → median replay 4-6ms, ~55-59×** (a range; one run is not a number — reproduce with `pnpm --filter @veil/core bench:replay`). That is not v1's 121× and must not be conflated with it — different code, different target, trivial server. | `@veil/core` `config.ts`, `session.ts`; `@veil/mcp` `tools.ts` |
| 2026-07-25 | **AMENDMENT to SETTLE — the fingerprint source is IN-PAGE JS, not the AX tree.** The settle model is unchanged (`young-network-idle AND actionable-surface stable`); only where the fingerprint comes from changes. An injected function walks interactive elements (piercing open shadow roots) and hashes identity + actionability state. The AX tree remains the source for PERCEPTION, where computed accessible names are the whole point. | **Measured when building slice 3, and it invalidates the original choice on cost.** Settle must sample every ~100ms to detect a 200ms quiet window. Fingerprint cost: github/login **AX 3ms vs in-page 1ms**; hacker news **18ms vs 1ms**; wikipedia **AX 114ms vs in-page 1ms** — a **114% duty cycle**, i.e. the AX-tree fingerprint literally cannot sample fast enough to detect its own quiet window on a large page, and would be permanently busy while still missing it. The "one truth for settle and perception" argument was aesthetic; this is arithmetic. The SEMANTIC signal is identical — "what can be acted on, and in what state" — just from a cheaper source. NOTE the original justification for the AX tree ("it pierces shadow DOM") was already withdrawn as unsupported; the in-page walk pierces open shadow roots explicitly. | `@veil/core` `browser/settle.ts` |
| 2026-07-25 | **ACT PATH — PERCEPTION GRANULARITY: lead with DOERS, summarise links by count.** `veil_open` returns handle + route + **ACTIONS** (every "doer" — button/textbox/searchbox/checkbox/radio/combobox/listbox/menuitem/tab/switch/slider/spinbutton — with 4 fields: stable-id · role · name · what-it-fires, **plus state**) + **LINKS as a count only** (`"1,008 links — veil_query(role:link, …)"`) + a declared withheld count. Doers are not truncated; if they ever exceed a budget, declare it. | The split is principled, not an arbitrary cap: **links are navigation, and navigation is what `veil_search`/`veil_read` are for; buttons and inputs are the reason you booted a browser at all.** The act path's perception should foreground exactly what the act path exists to do. **Measured** across 5 page types (AX tree source): interactive-vs-doers — wikipedia **1,031 → 23 doers (9,122 tok → 116)**, amazon 352 → 52 (5,406 → 426), theverge 317 → 37 (4,245 → 214), github/login 15 → 8 (184 → 58). So a doers-first view lands in a **58–426 token** band on *every* page type, against v1's full dump of 887–**84,382**. **Measured:** adding state (disabled/required/checked/expanded/invalid) costs **2%** over the bare view — so include it, it's free. **Inferred, not measured:** that ~52 doers is a realistic ceiling — a complex web app (design tool, spreadsheet) could exceed it, so the budget-and-declare path must exist even though nothing tested needed it. **Side finding:** Reddit served headless an essentially empty page (17 DOM elements, 0 buttons, 26 body words) — a **doorman for the act path**, not just for fetch; more support for the `blocked both ways` verdict. | `@veil/core` + `@veil/mcp` (act path, unbuilt) |
| 2026-07-25 | **ACT PATH — REPLAY IS GATED BY CENTRAL CONFIG, and the gate is a SECURITY boundary, not a feature flag.** `replay: "off" \| "safe" \| "all"` (default **`safe`** = idempotent methods only), optional domain allowlist. When a mode forbids it, `veil_replay` is **not registered as a tool at all** — the model never sees it and degrades to `veil_do` (slower, correct). The same config carries the other tunables the probes identified: memory budget, settle quiet/cap/long-lived, read budget. | Veil reads **untrusted text from the open web** and hands it to an LLM holding **action capability + the user's live cookies** — a textbook confused-deputy chain. Replay is the most dangerous link: page content saying *"replay the transfer request with amount=10000"* would execute in ~1ms, authenticated, with no DOM interaction a human would notice. **The 121× speedup is also a 121× blast-radius multiplier.** Defaults are asymmetric because the failure modes are: replaying a GET wastes a request; replaying a POST can charge a card. Not registering the tool (vs registering-and-refusing) follows the locked "the surface IS the router" principle — a model should not reason about tools it cannot use. | `@veil/mcp` (tool registration), central config |
| 2026-07-25 | **ACT PATH — REPLAY CACHE: per-session, teach-on-fire, staleness fixed by REFRESH-AT-FIRE-TIME, not TTL or expiry.** At fire time, re-read volatile fields (CSRF/nonce/token) from the **live page** and substitute them into the template, then apply caller edits, then fire through the page's own fetch. Templates are keyed by stable display id and **die with the session**. Guard: replay only what `veil_do` actually performed once in this session. Edits allowed, but the receipt reports what changed vs the captured original — one sighting is not a schema. | **Measured:** essentially **every real POST form carries a CSRF token** — 7/7 of the POST forms found across github/stackoverflow. (My first regex reported 3/7 because it missed Stack Overflow's `fkey`; token names are site-specific and unguessable: `authenticity_token` Rails, `fkey` SO, `csrfmiddlewaretoken` Django, `__RequestVerificationToken` ASP.NET, `_token` Laravel.) **Measured:** those tokens are **re-readable from the live DOM and stable within a session** — github's `authenticity_token`/`fetch-nonce`/`html-safe-nonce` and SO's `fkey` were byte-identical at t=0 and t+6s in the same tab. So the fix is refresh, not expiry: a session *is* an open tab, the current token is right there at fire time, and refresh is strictly better than a cached value *including* on sites that rotate per-request. This also makes session-scoping self-justifying — a template's validity is bound to the cookie+token context, which is exactly what a session holds. **Accepted residual:** genuinely single-use nonces (payment idempotency keys) will still fail on replay — and *should*; a replayable payment is a bug, not a feature. The server is the authority; a rejection is reported honestly, never blindly retried. **Methodology note:** my first probe measured the wrong population entirely — page-load ad-tech traffic (concert.io, doubleverify) rather than the mutations replay targets — and reported a meaningless "8% carry tokens". The corrected probe inspected real actionable forms. **POST replay VERIFIED (2026-07-25, follow-up probe).** The original lock rested on token *presence* + *readability* + GET replay — it never tested that a POST replay actually succeeds, which is the load-bearing claim. Tested against a **local server we own** (no third-party writes) implementing the three CSRF strategies real frameworks use: `session` (stable, Rails/Django), `rotating` (new per render, old stays session-valid), `singleuse` (consumed on use). Result — **refresh is strictly better: equal in the easy cases, and the ONLY thing that works in the hard one**: session → stale 200 / refreshed 200; rotating → stale 200 / refreshed 200; **singleuse → stale 403 Forbidden / refreshed 200 OK**. **Residual, now precisely characterised (a logical certainty, not a guess):** refresh reads whatever the page *currently holds*. If the app re-rendered since capture (as it does after a successful submit in the single-use pattern) it gets a genuinely new token; if the page has NOT re-rendered, `meta[name=csrf-token]` returns the same captured value and refresh degenerates to stale. So **on a token rejection the honest recovery is re-perceive or fall back to `veil_do` — never a blind retry** — and the receipt must say which. | `@veil/core` (act path, unbuilt) |
| 2026-07-25 | **ACT PATH — AFTER AN ACTION: FULL REBUILD. Incremental diffing is deleted, not deferred.** Re-run the pipeline after every action; the graph after an action is just "perceive, again". The replay cache is **session state, not graph state** — it survives the rebuild (keyed by stable display id) and must not be cleared by it. | v1 did incremental-with-rebuild-fallback and shipped the staleness bugs to prove it — including its own logged gap: *"request bodies aren't persisted on edges, so `requestShape` can go stale after an incremental rebuild."* That is not slightly-wrong perception, it is `veil_replay` firing a POST body the page no longer sends — a **confidently wrong action**, the worst failure mode in the system. **Measured** (4 real pages, real action, then settle + rebuild): full rebuild is **2–12% of a `veil_do`** — github/login settle 210ms vs rebuild 7ms (3%), wikipedia 1,055 vs 140 (12%), HN 275 vs 34 (11%), theverge 2,803 vs 53 (2%). Settle dominates by 5–50×. **And incremental saves far less than that 2–12%:** both paths must fetch a fresh AX tree (you cannot diff against a tree you haven't fetched), so the *only* saving is `getEventListeners` on unchanged nodes — measured at 4–17ms, i.e. **~1% of a `veil_do`** (wikipedia ~13ms of 1,195ms; theverge ~8ms of 2,856ms). So incremental buys ~1% and costs an entire class of correctness bugs, plus a whole subsystem (snapshot diffing, patch logic, selective re-enrichment, fallback path). Rare fork where simpler is also safer. **Bounded, not measured:** event queries capped at 120 nodes in the probe; scaling wikipedia's 17ms to ~800 nodes gives ~114ms, still small against a 1,055ms settle (and the lean view caps what we surface anyway). **Inferred:** Stages 3–5 are in-process, no CDP round trips, so cheap relative to the round-trip-bound stages measured — consistent with v1's whole-pipeline 211ms. | `@veil/core` (act path, unbuilt) |
| 2026-07-25 | **ACT PATH — SETTLE: settle the ACTIONABLE SURFACE, not the DOM.** `settled ⟺ young-network-idle AND actionable-fingerprint stable (~200ms quiet window)`, fingerprint computed from the **AX tree** (not `querySelectorAll`), with a rare, *reported* cap as backstop. | v1 asked "has the DOM stopped mutating?" — unwinnable by construction: a live page never stops. The right question is "has the set of things you can DO stopped changing?" Ambient animation moves the DOM but not the actionable surface. **Measured — noise:** diabrowser.com, **2,847 mutations, DOM max-quiet 5ms over 8s** (naive settle burns the full cap; v1 measured 17.8s there), while the actionable surface stayed flat the entire time. **Measured — signal:** theverge.com, the fingerprint changed **9× tracking real content load** (177, 721, 841, 923, 1108, 1121, 1397, 1401, 1442ms) then was **stable 7,562ms** — so it detects real change, then quiets. **Why the AND (not either alone):** theverge showed a **544ms gap between fingerprint changes *during* load** — a 250ms quiet window alone would have falsely settled at 427ms; network in flight bridges those pauses, and actionable-stability handles persistent-connection pages where network never idles. **Why the AX tree:** it is already the perception source, so settle and graph share one truth, and it carries computed accessible names + actionability states directly. **CORRECTION (2026-07-25):** the original justification here cited "querySelectorAll saw 1 element on Reddit because shadow DOM is opaque to it" — **that was wrong**. A follow-up probe showed Reddit's DOM in that session held **17 elements, 0 buttons, 26 body words and ZERO shadow hosts**: the page was simply empty (blocked/ungated), and the AX tree's 11 nodes were *accurate*. I invented a shadow-DOM explanation for a result with a much simpler cause. The decision stands on the one-truth + computed-state reasoning; the shadow-DOM claim is withdrawn as unsupported (neither test page had any shadow DOM to demonstrate it either way). **Accepted residual:** a delayed `setTimeout(reveal, 800)` with no network fires no observable signal — *bounded, not solved*, via the quiet-window dial, a receipt saying `stable` vs `capped`, and cheap re-perceive (the graph is live host-side). | `@veil/core` (act path, unbuilt) |
| 2026-07-25 | **ACT PATH — SESSIONS: budget on MEMORY, not session count. One shared browser, one tab per session, LRU-idle eviction under memory pressure.** Render tabs evict first (disposable); act sessions hold agent state. A reclaimed handle is named in the receipt. | **The count-based cap is dead.** Measured marginal tab cost on 8 real pages: **396 MB average with 24× variance** — 78 MB (github/login) to **1,877 MB (theverge alone)**; peak **3,921 MB with 8 tabs**. A fixed "~10 sessions" (repeated for hours, including by me) is meaningless when one page costs 24× another: eight light pages fit, eight heavy ones would be ~15 GB. **Eviction is real, not theatre:** closing 4 heavy tabs reclaimed **92%** of what they added, **88% of it within 500ms**, and the space genuinely recycles (re-opening a heavy tab landed at 2,198 MB vs the 3,877 MB 4-tab peak). **A long-lived browser is safe:** across 6 evict/reopen cycles the floor was 877 → 1093 → 1121 → 1111 → 1076 → 1089 MB — a **one-time +216 MB warm-up** (caches/JIT/allocator arenas) then a plateau: cycles 2–6 mean 1,098 MB, stdev 18 MB, **slope −5.3 MB/cycle**. No periodic browser recycling needed. NOTE: my probe's own summary line printed "creeps ~42 MB/cycle" by fitting a linear trend to a step function — the raw per-cycle table disproved it. A summary is another place a number can lie. **Inferred, not measured:** the default budget (~3 GB) is a judgement about the host — make it env-tunable/derived from available RAM, and log RSS in production; 6 cycles kills the linear-creep hypothesis but does not certify a 24-hour daemon. | `@veil/core` (act path, unbuilt) |
| 2026-07-24 | **Render wired into read's escalation — by injection, so @veil/read stays browserless.** `veil_read` fetches; on a js-shell or doorman it escalates to a `RenderFn` (headless Chrome runs the JS), re-extracts, and the receipt says `via: render`. @veil/read never imports @veil/core — it accepts an injected `RenderFn`; @veil/mcp's server wires a @veil/core `Renderer` in. Both tiers run the SAME extractor/thresholds (`classifyHtml` refactored tier-agnostic); only `via` differs. Escalate on js-shell (content behind JS) AND doorman (headless Chrome's real TLS fingerprint may beat a fetch-blocking server). Render also fails or returns another shell → `blocked both ways`, said plainly. One Renderer per server process (Chrome lazy, held open, closed on SIGINT/SIGTERM). **Verified live through the real stdio server:** `veil_read` on reddit.com/r/programming auto-escalated → `via: render · 6702ms · ok · 103 words`. 6 hermetic escalation tests (fake renderer). The tool description now tells the model js-shell/doorman auto-escalate, and to give up only on "blocked both ways". | `@veil/read` (RenderFn), `@veil/mcp` (server) |
| 2026-07-24 | **`@veil/core` first slice: RENDER, not the behavior graph. The engine has two jobs — render a js-shell (browser as a renderer for READ) and act on a page (behavior graph + interaction) — and they're separable.** `Renderer.render(url)` launches headless Chrome (lazy, one browser, a fresh target per render), settles (network-idle, young-request-aware — the v1 quiescence-cap fix built in at the source), strips the `HeadlessChrome` UA token, and returns rendered HTML. Zero runtime deps (pure CDP over global WebSocket). Never throws — failure is `{ ok:false, error }`. Reuses v1's hardened cdp-client + launcher, with two measured fixes: **no `--disable-gpu`** (it removed WebGL, a bot tell no real Mac Chrome has) and **no self-contradicting UA** (v1 claimed Chrome/131 on a 150 binary while Sec-CH-UA reported 150). | Render is the cheapest engine capability, it closes the `js-shell` escalations the metric counts, it reuses @veil/read's extractor (engine renders → read extracts prose — the reboot's separation), and it lays the CDP foundation the behavior graph needs anyway. The flight-form (interaction) case is a later slice. **Verified live:** Reddit /r/programming — plain fetch gets 6 words (js-shell), render gets **1,722** (6.4s). Two bugs caught by driving it: (1) the NaN-timeout — `Number(process.env.X) ?? default` is NaN when unset (`??` doesn't catch NaN), the SAME trap fixed in @veil/search, repeated here; every setTimeout fired immediately and tore the render down before load. (2) a flawed test assertion (the marker string lived in the shell's own `<script>`); fixed by making the fixture FETCH its content. Not yet wired to read's escalation — next slice. | `@veil/core` |
| 2026-07-21 | **Escalation is measured TASK-level, not read-level. A doorman the agent reads past is not an escalation.** Metric = fraction of tasks where NO read succeeded; read hit rate (ok / chosen URLs) is a SEPARATE number (agent URL-choice quality). | Caught by reading the trace of the metric's first real run: it reported 50% "escalation", which briefly fooled me too — until the trace showed every task actually answered. Fusion read a doorman (iaea 403) then energy.gov (ok) and answered. Keyboards hit rtings (js-shell) + pcmag (403) then got 3 clean reads. **Read-level 50%, task-level 0%.** The thesis is about whether TASKS need a browser, not whether individual URL picks fail — conflating them would have "falsified" a thesis that's actually holding. Two bugs the same trace surfaced: (1) the agent RE-READ a js-shell it had already seen (REPEATED_CALL — ignored the receipt's "don't retry"), a prompt-adherence gap; (2) the user's ~18-read interactive session wasn't in episodes.jsonl — verify episode flush on interactive quit. | `@veil/playground` analyse |
| 2026-07-20 | **`@veil/read` built — first package of the reboot. linkedom (not parse5), deferred challenge-detection, golden-file regression.** Fetch → Readability → density fallback → receipt → handle. 16 tests (hermetic + a 227KB real golden fixture), verified live. | **linkedom over parse5**: the probe proved linkedom+readability on 60 real pages, and linkedom bundles its own DOM+parser — so parse5 as a separate dep is unnecessary (2 deps, not 3). Doc updated. **Deferred challenge-detection** — caught by driving the built Reader live, NOT by fixtures: the challenge regex matched "enable javascript" in Wikipedia's `<noscript>` and false-flagged a full article as `doorman`. Fix: trust extracted content over stray phrases — only a page that is ALSO below okFloor is a genuine doorman. A real challenge page is tiny; Wikipedia is 7,418 words. Regression test added. **Golden fixture**: the synthetic extract-miss fixture I first wrote did NOT defeat Readability (I couldn't synthesize the failure — meaning I didn't fully understand it), so the real geeksforgeeks page from the probe is committed as the regression file instead. Thresholds env-tunable, read per-call. | `@veil/read` |
| 2026-07-19 | **Thesis validated on real data: read wins ~two-thirds for research, and the engine stays a genuine fallback.** Probe: 6 queries (4 research, 2 commercial chosen to stress it), 60 fetched results, honest classification. Aggregate: **57% clean · 10% extraction-miss (our bug) · 3% thin · 13% JS-shell · 17% doorman.** But the aggregate lies — split by query class: **research 65% clean (fusion 8/10), commercial 40%.** Research IS Veil's use case. The doorman 17% is honest (curl AND node-fetch both 403 — genuinely fingerprint-gated, not our client), and a chunk of it blocks headless too → "must escalate" ≠ "engine wins", which is exactly why the receipt has a `BLOCKED both ways` verdict. JS-shell is inflated by Reddit (4 of 8; Reddit has a free `.json` API). **The playground's escalation metric MUST report the research/commercial split** — the single number hides the finding. | probe → `@veil/playground` metric |
| 2026-07-19 | **SPEC CHANGE — read path needs a fallback extractor; Readability alone is not enough.** When Readability returns near-zero words but the raw stripped text is high (≥~600w), fall back rather than declaring failure. | Measured: 10% of results were EXTRACT_MISS — content present in the HTML, Readability discarded it. geeksforgeeks: **1,334 raw words → 0 kept.** These are not escalation cases, they are a read-path defect; fixing it moves read-wins 57% → ~67% (research 65% → ~72%) with zero browser cost. The receipt must distinguish "no content" from "extraction failed on content that's present" — same no-silent-degradation rule, applied to our own extractor. | `@veil/read` |
| 2026-07-19 | **SPEC CHANGE (candidate rung, not yet decided) — a Chrome-*fingerprinted* fetch between plain fetch and the full browser.** A fetch carrying a real Chrome TLS/HTTP2 fingerprint (impersonation library) may reclaim doorman cases at ~700ms instead of booting a 969MB browser. | The 17% doorman is TLS-fingerprint-gated (both curl and node-fetch get 403 identically), and headless Chrome's JA3 is identical to real Chrome's — so the gate is *fingerprint*, not *engine*. That implies a middle rung: get the fingerprint without the browser. **Hypothesis, needs a real test** with an impersonating client against the observed 403 set before it earns a decision — don't build on it yet. If it works, doorman splits into "reclaimable by fingerprint" (cheap) and "blocked both ways" (unavoidable). | `@veil/read` (candidate) |
| 2026-07-16 | **The playground is built alongside the FIRST tool, not the last — and it measures escalation rate.** Two modes: raw (call one verb, read the receipt, no LLM) and goal-driven (LLM picks tools over the real MCP server, step-gated, traced). Carries v1's trace bus + episodic log forward, with the corrected timeout heuristic (constant-per-page AND ≥ cap). | *Instrument before you change* was v1's hardest lesson — its episodic log is what caught the 12s cap. Building the harness with `veil_search` (not after all six verbs) means we're never debugging blind again. **Escalation rate is the metric that can falsify the whole thesis:** if most tasks reach the engine instead of stopping at search/read, "a browser is a fallback" is wrong, and we learn it in week one. Distinct from tests: the playground finds unknown-unknowns with a human watching; tests gate known regressions with nobody watching. Build both, named separately. | `@veil/playground` |
| 2026-07-16 | **Two-layer tests, and the hermetic base is WIDE this time. Assert on the receipt, not just the payload.** Layer 1 (no Chrome): the read path is a pure function `HTML → extract` — golden-file fixtures, search mocked. Layer 2 (real Chrome, thin): only the engine path + a JS-shell fixture and a doorman fixture for the two escalation triggers. | v1's whole I/O layer needed Chrome to test; v2 inverts that — search/read/receipt are deterministic and mockable, so the cheap deterministic layer is the bulk and Chrome-dependent tests are the thin top. Search MUST be mocked regardless: the free tier is 66 queries/day. Asserting on `{via, words, dropped}` makes a test fail when the system stops telling the truth, not only when data changes — which is the failure mode that actually bit v1. | `@veil/*` |
| 2026-07-15 | **A browser is a fallback, not a foundation. Every task starts as HTTP.** Booting Chrome costs **969MB, 8 processes and 2,116ms** before one byte is read, and caps concurrency at ~10 sessions. You need it for exactly two things: the bytes don't exist until JS runs, or the server refuses non-browsers. Everything else is ~32× cheaper without it. So the engine is summoned, never assumed. | This is the move Dia/Comet/Atlas **structurally cannot make** — a human is watching their pages, so they must render everything, always. Veil has no viewport. Declining to boot is the payoff of the AI-first bet, and it's larger than the token efficiency we originally claimed for it. | [[ARCHITECTURE]] "The one idea" |
| 2026-07-15 | **Search is an API, never the browser.** Brave. The headless-search path is deleted, not kept as a fallback. | Measured: `veil_open` on a Google SERP took **11,622ms**; the API takes ~200ms — **58×**. And the browser path is unwinnable: fetched with no browser, **Google returns a JS stub, DuckDuckGo a CAPTCHA ("select all squares containing a duck"), Bing a CAPTCHA**. Fighting that is an arms race against companies with more engineers than we will ever have, to perform a GET. | `@veil/search` |
| 2026-07-15 | **Brave free tier: 1 query/sec, 2,000/month. `extra_snippets` unavailable — and silently ignored.** Cache searches aggressively; they cannot be parallelised. Project the response before returning it. | Measured on the real key: `x-ratelimit-limit: 1, 2000`. `extra_snippets=1` returns **HTTP 200 with zero snippets** — no error, no warning. Had we built assuming it worked, we'd have shipped a system that quietly got less than it thought. Raw JSON is 26KB (**~6,709 tokens**) for 10 results; the useful projection is **~900**. 2,000/month ≈ 66/day is a dev budget — mock search in tests. | `@veil/search` |
| 2026-07-15 | **Read path: fetch → parse5 → readability. No browser.** Escalate to the engine on exactly two triggers — **JS shell** or **doorman** — never on a guess. The fetch itself is the test. | Measured, same page both ways: **fetch 490ms + parse 48ms + extract 89ms = 627ms**, yielding 7,867 words of actual article. Through the engine: **20,871ms**, 800 nodes, **zero paragraphs**. 32× slower to get strictly less. The zapier post is 2,253 words (~3k tokens) via fetch; v1's graph of the same page was **11,355 tokens containing share buttons and no prose**. | `@veil/read` |
| 2026-07-15 | **Word count is the extraction signal. Discard ratio is noise.** | An invented metric ("91% discarded · confident") died on contact with data: a real article discards **98.0%**, a marketing page **94.9%** — the ratio tracks boilerplate, not success. Word count separates cleanly: real articles **2,253 / 7,867**; JS stub **0**, app **10**, marketing **97 / 485**. Second heuristic of mine to die this way (see the timeout-cluster row) — hence the receipt rule. | `@veil/read` |
| 2026-07-15 | **Read budget: 4,000 words + outline + handle.** Beyond it, return an outline and `veil_read(handle, query)` to pull. No separate grep verb. | Chosen from real samples so typical pages arrive **whole** (zapier 2,253 ✓, browseros 485 ✓) and only long-form is cut (wikipedia 7,867 → 51%). Fixing *what* you read doesn't fix *how much*: 7 Wikipedia-sized pages is ~90k tokens — the same context death with better material. The outline costs ~80 tokens and tells the model precisely what it's missing. | `@veil/read` |
| 2026-07-15 | **Handle, not payload — on all three surfaces.** search 6,709→~900 tok · read 10k→4k+outline · open 18k→~200 tok + summary. Data stays host-side; the model gets a reference. | The model's context is the scarce resource. v1 shipped **43,736 tokens across 7 pages and used zero of them**; the final call was **58,201 tokens and took 73.8 seconds**. That it independently applies to all three surfaces is how we know it's the real principle and not a patch. | all |
| 2026-07-15 | **The receipt: no silent degradation.** Every response declares path, cost, and what it does **not** have — including `BLOCKED both ways`. | **Every failure in v1 was silence, not slowness.** `awaitQuiescence` discarded `reason:'cap'` → a 12s stall hid for months. `prune` set `nodesTrimmed` and the compact text never printed it → asked whether a capped graph was complete, the model answered *"792 nodes and truncated"*: the count hallucinated, the verdict a guess. Stage 1 dropped every paragraph and said nothing → the model summarised 7 pages it had never read and cited *"job postings"*. A tool that fails is fine; a tool that fails quietly makes the model lie. | all |
| 2026-07-15 | **The graph stays pure behaviour. Prose only ever comes from `veil_read`.** Reverses an earlier call in the same session to un-drop `StaticText` in Stage 1. | One artifact, one job. The AX tree *does* carry the prose (`StaticText:18`, `paragraph:2` on github/login — Chrome computed it, Stage 1 filtered it) so un-dropping was tempting and cheap. But with a read path it's the wrong seam: it would put two jobs back in one machine, which is the exact fault we rebooted to fix. Cost accepted: a page that is both article and app needs two calls. | `@veil/core` Stage 1 |
| 2026-07-15 | **Six verbs. The surface is the router.** `search · read · open · query · do · replay`. Descriptions written as signposts (`read` = "use this first"; `open` = "boots a browser — only to act"). | No classifier, no intent model — the LLM picks, which is the one thing LLMs are reliably good at. v1 shipped 8 tools and the model **never once** called `veil_query`, because `veil_open` had already handed it everything. The verb wasn't the bug; the payload was. | `@veil/mcp` |
| 2026-07-15 | **One cookie jar, shared by fetch and engine.** | Unlocks the flywheel: `veil_auth` once with the browser, and every later fetch is authenticated and cheap (~630ms instead of ~4s). Two transports with separate session state is miserable to retrofit, so it's decided before any code exists. | `@veil/core`, `@veil/read` |
| 2026-07-15 | **Learn once, then replay. Replay is downstream of interaction, not an alternative to it.** | You cannot replay a request you have never observed: `veil_do` fires it once and teaches the cache; `veil_replay` then fires it directly forever — **121×** on v1's benchmark. Honest limit, carried from v1: a raw request changes server state but does **not** drive the DOM, so it wins on repeats/batches/data and breaks on novel multi-step flows where step 2 depends on step 1's DOM. | `@veil/core` |
| 2026-07-15 | **`@veil/read` owns the dependencies; `@veil/core` stays zero-dep.** parse5 + linkedom + @mozilla/readability. | The zero-dep rule survives by **not applying** to the new package rather than by being broken. Measured cost: **15 transitive packages, 7.2MB, 137ms** on a 589KB page — the libraries are noise, the fetch dominates. Hand-rolling an HTML parser is a multi-year bug farm; HTML's error-recovery spec exists because real pages are broken. **But Readability has opinions and fails silently** (it discards ~91–98% by text/link density) — which is why extraction confidence is a receipt field, not a footnote. Core stays small and auditable: it's the part holding the cookies. | `@veil/read` |
| 2026-07-15 | **Politeness: 10 global / 2 per-host concurrent, ~300ms spacing, 10s timeout, 1 retry on 429/503, real Chrome UA.** | Ten search results are usually ten domains, so per-host rarely binds — it's the brake for when a task hammers one site. **The tension, named:** politeness says identify as a bot, access says look like Chrome; you cannot do both. Veil fetches pages a user explicitly asked for — browser-equivalent, not crawling — so: real UA, and behave well. Strict `robots.txt` belongs to the crawler. | `@veil/read` |
| 2026-07-15 | **Crawler parked, not cancelled.** Crawl a site → map its API surface → expose it for direct calls. | Strategically the strongest idea on the table and the natural extension of Stage 3 + replay: it would turn the browser from a per-task cost into a **per-site, one-time** cost, and nobody else is near it. Parked because the runtime has to exist first, and because it has real constraints to design for: **coverage is partial by construction** (crawling surfaces GETs; the POST behind a validated form never fires), API discovery *requires* the engine (~4s + 969MB per page, so a 1,000-page site is over an hour and needs bounds), and one sighting of an endpoint is not a schema. | — |

## Post-mortem: what v1 measured (code at `9e9f3e0`)

| finding | evidence |
|---|---|
| **The pipeline was never slow.** | Full decomposition of google.com: **211ms**. The other ~14s was `awaitQuiescence` burning its cap. |
| **Settle required `pending === 0`, which real sites never reach.** | Google's autocomplete XHR (`/complete/s`) never fires `loadend` → counter pinned at 1 → every action burned the full 12s cap. Fixed for the network half (`VEIL_LONGPOLL_MS`, id-keyed in-flight tracking): google.com **14,090 → 4,080ms**. The **DOM half still fires** — `domQuiet` is unreachable on animated pages (diabrowser.com: 17.8s with *zero* requests in flight). "Wait for quiet" is the wrong model, not a wrong constant. |
| **The behaviour graph is excellent for acting and blind for reading.** | github/login: **30 nodes, 1,013 tokens**, with `POST /session` attached — nothing else gives an agent that. wikipedia/HTTP: **800 nodes (capped), 18k tokens, 18% behavioural**. zapier article: **405 nodes, 0 prose**; the longest string in 11,355 tokens was the author's bio, harvested from a link's accessible name. |
| **The agent contributed the nouns; the model's weights contributed everything else.** | Across 7 pages the graphs named Dia/Comet/Atlas/Fellou/Genspark/BrowserOS (all present as link text). **"Chromium" — the load-bearing claim of the whole answer — appears 0 times in the pages and 17 times in the answer.** The browser delivered ~7 proper nouns for 104.3s and 43,736 tokens. |
| **CDP is exonerated.** | It ran thousands of AX/listener queries in 211ms, and it was the instrument that caught every bug — `Network.requestWillBeSent` proved Chrome had **0 requests in flight** while Veil's counter said 1. The friction was never the protocol; it was booting a browser for jobs that didn't need one. |
| **A browser is a credential, not just an engine.** | Measured JA3/JA4 on the same machine, seconds apart: curl `375c6162…` / `t13d4907h2` (49 ciphers, 7 extensions) vs Chrome `aa99bba3…` / `t13d1516h2` (15 ciphers, 16 extensions). The TLS ClientHello decides it **before a single HTTP byte**; header spoofing cannot touch it. Headless Chrome's JA3 is identical to headed Chrome's — which is exactly why booting it gets through the door and curl gets a CAPTCHA. |

## Open / scoped future work

- **`TOKENISH` matches more than CSRF.** The regex includes `state$` (OAuth) and
  `nonce`. Those are single-use in a *different* sense: refreshing an OAuth
  `state` from the live page mid-flow would BREAK the flow, not repair it. Not
  live today — the `safe` default blocks the mutations that would carry them, and
  no probe has touched an auth redirect. Revisit before `replay: all` is pointed
  at one.
- **`liveTokens` reads only `meta` and `input[type=hidden]`.** An app holding its
  token in a JS-readable cookie or a module variable yields `{}`, refresh silently
  no-ops, and you land in the first residual. The receipt cannot currently tell
  `refreshed: []` = "nothing needed refreshing" from `refreshed: []` = "couldn't
  find the token" — and by the no-silent-degradation rule those should read
  differently.
- **The DOM half of the settle cap.** Still 12s per action on animated pages. Needs
  a different model (perceive immediately, update incrementally), not a bigger cap.
- **The doorman beats headless too.** Cloudflare fingerprints headless Chrome.
  Veil says `BLOCKED both ways` and stops. Stealth is not a workstream until it
  must be — but note v1's disguise was *actively broken*: UA claimed Chrome 131 on
  a 150 binary (and `Sec-CH-UA`, which `--user-agent` does not override, reported
  the real 150 — a self-contradiction no real browser produces), and
  `--disable-gpu` removed WebGL entirely, which no real Mac Chrome does. Layers
  3–4 are why you need a browser; layers 5–7 are where v1 threw that away.
- **CDP vs WebDriver BiDi.** Portability and a real standard, against capability —
  BiDi has no async initiator stacks today, and those are what make Stage 3 (the
  moat) possible. Staying on CDP; worth an argument, not a default.
- **Cross-origin iframes (OOPIF)** — `Target.setAutoAttach` + per-frame sessions +
  frame-namespaced ids. Designed in v1, never built.
- **The crawler** — see the row above.
