{
  "name": "@veil/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "veil": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts src/daemon-server.ts --format esm --sourcemap",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@veil/core": "workspace:*"
  }
}
