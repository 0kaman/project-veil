{
  "name": "@veil/mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "veil-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@veil/core": "workspace:*",
    "@veil/server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.17"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "vitest": "^3.1.1",
    "typescript": "^5.8.3"
  }
}
